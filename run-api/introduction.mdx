---
title: 'Run API - Introduction'
description: 'This section will describe **Run API** known also as **Endpoints**,
it will let you design and ship customized API tailored for your needs.'
sidebarTitle: Introduction
---

<Note>
    This documentation in only used for purpose of Run API, [Platform API](/platform-api/introduction) has different section.
</Note>

# Concept

<img
    className="block dark:hidden"
    src="/run-api/images/introduction.png"
    alt="RevoEngine - Run API Introduction"
    title="RevoEngine - Run API Introduction"
    style={{ border: "2px solid black" }}
/>

<img
    className="hidden dark:block"
    src="/run-api/images/introduction.png"
    alt="RevoEngine - Run API Introduction"
    title="RevoEngine - Run API Introduction"
    style={{ border: "2px solid black", filter: "invert(1)" }}
/>

<Steps>
    <Step title="Firstly">
        A client sends HTTP request to **Run API Endpoint**.
    </Step>
    <Step title="Secondly">
        Validates `Token` & `Instance` based on [Authentication](/run-api/authentication) mechanism.
    </Step>
    <Step title="Moreover">
        We are matching requested `Method` and `Path` against configured **Active** `Endpoints`, then we validate request against [Guard](/run-api/guard) if configured and execute attached [Component](/components) if configured.
    </Step>
    <Step title="Lastly">
        We return [Response](/run-api/) together with [HTTP Code](/run-api/http-codes).
    </Step>
</Steps>
